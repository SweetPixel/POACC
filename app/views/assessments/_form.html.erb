<%= form_for(@assessment) do |f| %>
  <% if @assessment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@assessment.errors.count, "error") %> prohibited this assessment from being saved:</h2>

      <ul>
      <% @assessment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Complaints %><br>
    <%= f.text_area :Complaints %>
  </div>
  <div class="field">
    <%= f.label :Clinical %><br>
    <%= f.text_area :Clinical %>
  </div>
  <div class="field">
    <%= f.label :Investigation %><br>
    <%= f.text_area :Investigation %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
