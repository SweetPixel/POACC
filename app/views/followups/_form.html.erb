<%= form_for(@followup) do |f| %>
  <% if @followup.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@followup.errors.count, "error") %> prohibited this followup from being saved:</h2>

      <ul>
      <% @followup.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Date %><br>
    <%= f.date_select :Date %>
  </div>
  <div class="field">
    <%= f.label :ClinicalPicture %><br>
    <%= f.text_area :ClinicalPicture %>
  </div>
  <div class="field">
    <%= f.label :Plan %><br>
    <%= f.text_area :Plan %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
